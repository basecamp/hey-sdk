// Generated by hey-sdk-generator from openapi.json and behavior-model.json. DO NOT EDIT.

/// CalendarYear — the grid a year is drawn as. A year carries one entry per day plus the
/// events that span more than one, not every recording it holds: a year's worth of
/// expanded occurrences is not something a client asks for by opening a year.
public struct CalendarYear: Codable, Sendable, Equatable {
    /// ISO 8601 date-time timestamp (overrides restJson1 epoch-seconds default)
    public var startsAt: String
    /// ISO 8601 date-time timestamp (overrides restJson1 epoch-seconds default)
    public var endsAt: String
    /// "year"
    public var kind: String
    /// Days between the reader's week start and January 1st, so the grid lines up
    public var paddingDaysCount: Int32
    public var days: [CalendarYearDay]
    /// All-day and multi-day events, oldest first
    public var spannedEvents: [Recording]

    public init(
        startsAt: String,
        endsAt: String,
        kind: String,
        paddingDaysCount: Int32,
        days: [CalendarYearDay],
        spannedEvents: [Recording]
    ) {
        self.startsAt = startsAt
        self.endsAt = endsAt
        self.kind = kind
        self.paddingDaysCount = paddingDaysCount
        self.days = days
        self.spannedEvents = spannedEvents
    }

    enum CodingKeys: String, CodingKey {
        case startsAt = "starts_at"
        case endsAt = "ends_at"
        case kind
        case paddingDaysCount = "padding_days_count"
        case days
        case spannedEvents = "spanned_events"
    }
}
