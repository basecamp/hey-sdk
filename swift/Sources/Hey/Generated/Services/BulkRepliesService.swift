// Generated by hey-sdk-generator from openapi.json and behavior-model.json. DO NOT EDIT.

/// Service for BulkReplies operations.
public final class BulkRepliesService: BaseService, @unchecked Sendable {
    /// Send one reply to every entry. Answers what was sent, not the replies themselves:
    /// delivery is queued, and delayed while undo is still possible.
    ///
    /// - Parameters:
    ///   - body: Request body
    public func create(body: BulkReplyRequestContent) async throws -> CreateBulkReplyResponseContent {
        var operation = try client.operation(Routes.createBulkReply, [])
        try operation.json(body)
        return try await client.send(operation)
    }

    /// Work out which entries a bulk reply would answer. HEY replies to the last replyable
    /// entry of each thread, skipping threads with no reply address, so the postings you hold
    /// are not the entries you send to — this resolves them.
    ///
    /// - Parameters:
    ///   - postingIds: The `posting_ids` query parameter
    public func newBulkReply(postingIds: String) async throws -> NewBulkReplyResponseContent {
        var operation = try client.operation(Routes.newBulkReply, [])
        operation.query("posting_ids", postingIds)
        return try await client.send(operation)
    }
}
